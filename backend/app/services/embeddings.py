"""Chunking + embedding helpers (OpenAI text-embedding-ada-002 -> pgvector).

TODO(Phase 1, Week 3): implement chunk_text() and embed_chunks(), called by
the crawler ingestion step to populate the `embeddings` table.
"""
